# Normalize line endings so the repo behaves the same on Windows, macOS and Linux.
* text=auto eol=lf

# Treat these as binary (never line-ending-normalized / diffed as text).
*.png binary
*.jpg binary
*.pkl binary
*.ico binary
